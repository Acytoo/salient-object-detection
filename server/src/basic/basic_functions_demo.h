#include "segment_image.h"

#include <iostream>
// demo for some basic operations during salieny- cut
namespace basic_functions_demo {
  int segment_demo(const std::string& ori_path, std::string& res_path);
  // int resize_demo(const std::string& ori_path, std::string& res_path);
  int bgr2lab_demo(const std::string& ori_path, std::string& res_path);
}
